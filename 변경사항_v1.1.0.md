# v1.1.0 — 대용량 일괄 전송 시 데몬 멈춤 수정 + 폴더명 필터

## 1. 멈춤(행) 원인

대용량 이미지를 한 번에 투입하면 아래 연쇄로 데몬이 멈췄다.

1. **백엔드**: `FileAgentWsHandler` 가 파일 전체를 힙(`ByteArrayOutputStream`)에 누적
   → 대용량 × 다중 세션이면 GC 폭주/OOM 로 수신 처리 급감.
2. **백엔드**: S3 PUT 풀 큐(256)가 차면 `CallerRunsPolicy` 로 **WS 수신 스레드가 직접 PUT**
   → 그동안 소켓을 안 읽음 → TCP 수신버퍼 가득.
3. **에이전트**: `WsClient` 의 소켓에 **send 타임아웃이 없어** `ws.send()` 가 무한 블록
   → 전송 스레드가 `_send_lock` 을 쥔 채 영원히 대기 = **데몬 전체 멈춤**.

## 2. 수정 내용

### 에이전트 (`agent.py`)
- WS 소켓에 **send 타임아웃** 적용 (`ws_send_timeout`, 기본 60초, 최소 5초).
  초과 시 연결을 끊고 재연결 후 해당 파일부터 재전송(at-least-once) — 더 이상 무한 블록 없음.
- 파일 전송 도중 실패하면(타임아웃 포함) 연결을 강제로 끊어 백엔드의
  `file_begin` 만 도착한 반쪽 상태를 리셋.

### 백엔드 (`FileAgentWsHandler`, `AgentToS3Executor`)
- 수신 파일을 힙 대신 **임시 파일(디스크)에 스풀** → 파일 크기와 무관하게 힙 사용 일정.
- `file_begin` 의 size 와 실제 수신 바이트 **크기 검증** (불일치 시 폐기 — 손상 업로드 방지).
- S3 PUT 은 스풀 파일에서 스트리밍(`RequestBody.fromFile`), PUT 큐 256 → **4096**
  (이제 큐에 경로만 담겨 힙 부담 없음 → CallerRuns 로 수신이 멈추는 빈도 대폭 감소).
- 연결 끊김/미완료 파일의 임시 스풀 자동 정리.

## 3. 폴더명 필터 (신규)

감시 루트 아래 "폴더 이름"에 fnmatch 패턴(`*`, `?`, `[]`, 대소문자 무시)을 적용해
조건에 맞는 폴더 안의 파일만 감시/전송한다. `config.json` 에 추가:

```json
{
  "watch_dir": "C:/S3_TEST",
  "folder_include": ["CAM_*", "OK_*"],
  "folder_exclude": ["temp", "_*"]
}
```

- `folder_include`: 지정 시 경로 중 한 폴더라도 패턴과 일치해야 처리.
  (지정하면 루트 바로 아래 파일은 폴더가 없으므로 제외됨)
- `folder_exclude`: 일치하는 폴더 안의 파일은 제외. include 보다 우선.
- 미지정이면 기존과 동일하게 전부 처리 (완전 하위호환).
- watchdog 이벤트·시작 시 기존 파일 스캔(`emit_existing_on_start`)·`/files` 목록 모두에 적용.

전체 예시: `config.folder-filter.example.json`

## 4. 데몬화(창 제거) + 배포 최소화

- 빌드가 `--noconsole` 로 변경 → 새 exe 는 **콘솔 창 없이 백그라운드로만** 실행된다.
  로그는 exe 옆 `agent.log` 로 확인 (`--noconsole` 대응 로깅 가드 추가).
- `dist\` 는 실행에 필요한 4개만 유지: `file-agent.exe`, `config.json`, `install.bat`, `uninstall.bat`.
  (빌드 스크립트도 이 구성으로 스테이징하도록 수정 — README/예제/ps1 은 dist 에서 제외)
- **부팅 시 자동 실행**: `install.bat` 더블클릭(자동 관리자 상승) = `file-agent.exe --install`.
  작업 스케줄러에 SYSTEM 계정으로 등록 — **부팅 시(AtStartup) + 로그인 시(AtLogOn)** 트리거,
  죽으면 1분 간격 3회 자동 재시작, 중복 실행 방지. 이미 구현돼 있던 기능으로 그대로 사용하면 된다.

## 5. 적용 방법

1. **에이전트 재빌드**: `빌드.bat` 실행 → `dist\file-agent.exe` 교체 (v1.1.0 + 창 없는 빌드).
2. **백엔드 재빌드/재기동**: `gradlew build` 후 배포.
3. 필요 시 `config.json` 에 `ws_send_timeout`, `folder_include/exclude` 추가.
4. 현장 PC 에는 `dist` 4개 파일만 복사 → `install.bat` 실행.

## 6. 운영 권장값 (현장 대용량 환경)

```json
{
  "ws_senders": 4,
  "ws_send_timeout": 60,
  "quiet_period_seconds": 1.0
}
```

- 데몬을 자주 재시작하는 환경이면 `emit_existing_on_start` 로 인한 전량 재전송을 피하도록
  백엔드 S3 노드에 `skipExisting=true` 설정 권장.
